# Assignment No. - 7

## Title

Stored Procedures or Function with Cursors.

## Problem Statement

Create and execute stored procedures / function using cursors.

## Objective

- To learn procedure and function in Pl/SQL. - To learn concept of cursor and types of cursor. - To implement cursor in PL/SQL.

## Theory

### CURSOR

For the processing of any SQL statement, database needs to allocate memory. This memory is called context area. The context area is a part of PGA (Process global area) and is allocated on the oracle server. A cursor is associated with this work area used by ORACLE, for multi row queries. A cursor is a handle or pointer to the context area.The cursor allows to process contents in the context area row by row.

THERE ARE TWO TYPES OF CURSORS.

Implicit cursor:-Implicit cursors are defined by ORACLE implicitly. ORACLE defines implicit cursor for every DML statements. Explicit cursor:-These are user-defined cursors which are defined in the declaration section of the PL/SQL block. There are four steps in which the explicit cursor is processed. • Declaring a cursor • Opening a cursor • Fetching rows from an opened cursor • Closing cursor General syntax for CURSOR:-

```sql
DECLARE
    Cursor cursor_name IS
        select_statement
        or
        query;
BEGIN
    Open cursor_name;
    FETCH CURSOR_NAME INTO LIST_OF_VARIABLES;
    CLOSE CURSOR_NAME;
END;
```

Where

### PL/SQL Cursor

When an SQL statement is processed, Oracle creates a memory area known as context area. A cursor is a pointer to this context area. It contains all information needed for processing the statement. In PL/SQL, the context area is controlled by Cursor. A cursor contains information on a select statement and the rows of data accessed by it. A cursor is used to referred to a program to fetch and process the rows returned by the SQL statement, one at a time. There are two types of cursors

- Implicit Cursors - Explicit Cursors

### 1. PL/SQL Implicit Cursors

The implicit cursors are automatically generated by Oracle while an SQL statement is executed, if you don't use an explicit cursor for the statement. These are created by default to process the statements when DML statements like INSERT, UPDATE, DELETE etc. are executed.Orcale provides some attributes known as Implicit cursor's attributes to check the status of DML operations. Some of them are: %FOUND, %NOTFOUND, %ROWCOUNT and %ISOPEN. For example: When you execute the SQL statements like INSERT, UPDATE, DELETE then the cursor attributes tell whether any rows are affected and how many have been affected. If you run a SELECT INTO statement in PL/SQL block, the implicit cursor attribute can be used to find out whether any row has been returned by the

SELECT statement. It will return an error if there no data is

selected.The following table soecifies the status of the cursor with each of its attribute.

### PL/SQL Implicit Cursor Example

#### Create analysis_jobs table and have records

```text
JOB_ID  SESSION_ID  JOB_KIND  JOB_STATUS
9001    501         FACE      RUNNING
9002    501         GENERAL   RUNNING
9003    502         FACE      PENDING
```

Let's execute the following program to update the table and change status of each VisionX job for session 501 to

FAILED.

Here, SQL%ROWCOUNT attribute is used to determine the number of rows affected.

```sql
DECLARE
total_rows number(2);
BEGIN
UPDATE analysis_jobs SET job_status = 'FAILED' WHERE session_id = 501;
IF sql%notfound THEN dbms_output.put_line('no VisionX jobs updated');
ELSIF sql%found THEN total_rows := sql%rowcount;
dbms_output.put_line( total_rows || ' VisionX jobs updated ');
END IF;
END;
/
```

#### Output

```text
2 VisionX jobs updated
PL/SQL procedure successfully completed.

Now, if you check the records in analysis_jobs table, you will find that the rows are updated.
select * from analysis_jobs;

JOB_ID  SESSION_ID  JOB_KIND  JOB_STATUS
9001    501         FACE      FAILED
9002    501         GENERAL   FAILED
9003    502         FACE      PENDING
```

### PL/SQL Explicit Cursors

The Explicit cursors are defined by the programmers to gain more control over the context area. These cursors should be defined in the declaration section of the PL/SQL block. It is created on a SELECT statement which returns more than one row. Following is the syntax to create an explicit cursor: Syntax of explicit cursor Following is the syntax to create an explicit cursor: CURSOR cursor_name IS select_statement;; Steps:` You must follow these steps while working with an explicit cursor. - Declare the cursor to initialize in the memory. - Open the cursor to allocate memory. - Fetch the cursor to retrieve data. - Close the cursor to release allocated memory.

### Declare the cursor

It defines the cursor with a name and the associated SELECT statement.

#### Syntax

```sql
CURSOR name IS
SELECT statement;
```

### 1. Open the cursor

It is used to allocate memory for the cursor and make it easy to fetch the rows returned by the SQL statements into it.

#### Syntax

```sql
OPEN cursor_name;
```

### 2. Fetch the cursor

It is used to access one row at a time. You can fetch rows from the above-opened cursor as follows.

#### Syntax

```sql
FETCH cursor_name INTO variable_list;
```

### 4. Close the cursor

It is used to release the allocated memory. The following syntax is used to close the above-opened cursors.

#### Syntax

```sql
CLOSE cursor_name;
```

### PL/SQL Explicit Cursor Example

Explicit cursors are defined by programmers to gain more control over the context area. It is defined in the declaration section of the PL/SQL block. It is created on a SELECT statement which returns more than one row. Let's take an example to demonstrate the use of explicit cursor. In this example, we are using the already created ANALYSIS_RESULTS table.

#### Create analysis_results table and have records

```text
RESULT_ID  SESSION_ID  RESULT_KIND  RESULT_STATUS  SUMMARY
1          501         FACE         COMPLETED      Recognized Pratik
2          501         GENERAL      COMPLETED      Indoor operator-console scene
```

#### Create procedure

Execute the following program to retrieve the session id, result kind and summary.

```sql
DECLARE
c_session_id analysis_results.session_id%type; c_kind analysis_results.result_kind%type;
c_summary analysis_results.summary%type;
CURSOR c_results is
SELECT session_id, result_kind, summary FROM analysis_results;
BEGIN
OPEN c_results;
LOOP
FETCH
c_results
into
c_session_id,
c_kind,
c_summary; EXIT
WHEN
c_results%
notfound;
dbms_output.put_line(c_session_id || ' ' || c_kind || ' ' || c_summary);
END LOOP;
CLOSE c_results;
END;
/
```

#### Output

```text
501 FACE Recognized Pratik
501 GENERAL Indoor operator-console scene
```

### DRAWBACKS OF IMPLICIT CURSORS

Even if your query returns only a single row, you might still decide to use an explicit cursor. Theimplicit cursor has the

The implicit cursor has the following drawbacks:

- It is less efficient than an explicit cursor.
- It is more vulnerable to data errors.
- It gives you less programmatic control.

The following sections explore each of these limitations to the implicit cursor.

## Conclusion

Thus we have studied the concept of cursor in PL/SQL procedure / function and implemented in SQL.

## FAQ

1. What is procedure in SQL?
2. What is function in SQL?
3. Explain the cursor operation.
4. What are types of cursors?
